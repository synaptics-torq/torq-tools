# SPDX-License-Identifier: Apache-2.0
# SPDX-FileCopyrightText: Copyright © 2025 Synaptics Incorporated.

# Moonshine Streaming requires the `moonshine_streaming` model class, which was added to
# Hugging Face Transformers in the 5.5.x series.
transformers==5.12.1
# Source export builds PyTorch wrapper modules and uses torch.onnx (dynamo) export.
# For a CPU-only install:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0
# Required by torch.onnx.export(dynamo=True).
onnxscript==0.7.1
# Validation: audio loading + resampling to 16 kHz.
soundfile==0.14.0
scipy==1.18.0
