# Graph Edit Test Data

Persistent reusable graph-edit fixtures belong here. Tests should write generated ONNX and NumPy artifacts to pytest `tmp_path` unless an artifact is intentionally stable and shared across test modules.
